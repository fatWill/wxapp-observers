/**
 * 绑定this指向的key值
 */

// 页面绑定的key值
export const pageNormalKey = ["methods", "events"]

// 页面的生命周期
export const pageLifetimeKey = ["onLoad", "onReady", "onShow", "onHide", "onUnload"]

// 组件的生命周期
export const componentsLifetimeKey = ["created", "attached", "ready", "moved", "detached", "error"]

export const lifetimes = "lifetimes";

export const watch = "observers";